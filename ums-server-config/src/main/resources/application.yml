server:
  port: 8888
spring:
  application:
    name: ums-server-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/marcustrng/uth-management-system.git
          search-paths: configuration
          default-label: master
